const workerService = require('./worker.service')
const delegateFrontend = require('./delegate-frontend.service')
const incomeFrontend = require('./income-frontend.service')

module.exports = { workerService, delegateFrontend, incomeFrontend }
