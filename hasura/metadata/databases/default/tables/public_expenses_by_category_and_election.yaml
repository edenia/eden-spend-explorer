table:
  name: expenses_by_category_and_election
  schema: public
select_permissions:
  - role: guest
    permission:
      columns:
        - category
        - election
        - total_eos_amount
        - total_usd_amount
      filter: {}
