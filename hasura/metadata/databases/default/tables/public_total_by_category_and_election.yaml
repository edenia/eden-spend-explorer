table:
  name: total_by_category_and_election
  schema: public
select_permissions:
  - role: guest
    permission:
      columns:
        - amount
        - category
        - election
        - type
        - usd_total
      filter: {}
      allow_aggregations: true
