table:
  name: total_claimed_and_unclaimed
  schema: public
select_permissions:
  - role: guest
    permission:
      columns:
        - category
        - amount
        - usd_total
      filter: {}
      allow_aggregations: true
