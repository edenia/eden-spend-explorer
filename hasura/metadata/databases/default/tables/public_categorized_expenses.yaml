table:
  name: categorized_expenses
  schema: public
select_permissions:
  - role: guest
    permission:
      columns:
        - election
        - amount
        - usd_total
      filter: {}
      allow_aggregations: true
