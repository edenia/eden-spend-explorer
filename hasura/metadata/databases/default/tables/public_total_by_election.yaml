table:
  name: total_by_election
  schema: public
select_permissions:
  - role: guest
    permission:
      columns:
        - amount
        - election
        - type
        - usd_total
      filter: {}
      allow_aggregations: true
