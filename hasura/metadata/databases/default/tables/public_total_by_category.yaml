table:
  name: total_by_category
  schema: public
select_permissions:
  - role: guest
    permission:
      columns:
        - amount
        - category
        - type
        - usd_total
      filter: {}
      allow_aggregations: true
