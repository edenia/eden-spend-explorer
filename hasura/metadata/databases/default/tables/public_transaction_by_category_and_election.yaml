table:
  name: transaction_by_category_and_election
  schema: public
select_permissions:
  - role: guest
    permission:
      columns:
        - amount
        - category
        - delegate_payer
        - election
        - exchange_rate
        - recipient
        - type
        - usd_total
      filter: {}
      allow_aggregations: true
