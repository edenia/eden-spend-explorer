table:
  name: categorized_expenses_by_delegate
  schema: public
select_permissions:
  - role: guest
    permission:
      columns:
        - amount
        - delegate_level
        - delegate_payer
        - election
        - exchange_rate
        - usd_total
      filter: {}
      allow_aggregations: true
